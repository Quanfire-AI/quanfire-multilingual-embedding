# Quanfire embedding models — catalogue & naming

Models are trained on the GPU box and published under the
[`quanfire-ai`](https://huggingface.co/quanfire-ai) org on Hugging Face. This file
is the single source of truth for how they are named and what exists.

## Public model id

```
quanfire-ai/<pillar>-<domain>-<scope>
```

The version is **not** in the name — it lives in the HF git tag / revision, so the
id stays stable and consumers pin a revision (as the adapters already pin their
base checkpoint's revision).

| Field | Meaning | Values |
|---|---|---|
| `pillar` | which capability | `embed` (bi-encoder retriever, this project) · `rerank` (cross-encoder that reorders a retriever's top-k) · `gen` (reserved: generative pillar) |
| `domain` | what it is tuned for | `general` · `legal` · `statute` · `finance` · `medical`* · `gov` |
| `scope` | language / region reach | `multi` (broadly multilingual) · `indic` · `in` (India, multi-script) · `en` · an ISO code |

`*` reserved — not yet trained. Add a domain/scope code here **before** publishing a
model that uses it, so the registry stays closed.

"CA firms" maps to `domain = finance` (accounting, tax, audit, filings).

### Version tags

`vMAJOR.MINOR.PATCH` on the HF repo:
- **MAJOR** — retrain that changes behaviour incompatibly (new base, new objective).
- **MINOR** — better weights, same contract (more data, more epochs).
- **PATCH** — card/metadata only.

Every adapter also pins the exact upstream revision of its frozen base checkpoint.

## Internal run id (box only — never published)

Training runs on the box carry the hyper-parameters the public id hides:

```
<domain>-<scope>-a<AA>s<SS>[-e<EPOCHS>][-<note>]
```

e.g. `legal-indic-a70s30-e3` — the `a70s30` blend and epoch count that produced a
release. At publish time the run id is recorded in the model card; the public repo
id stays clean.

## Catalogue

| Public id | Base | Status | Internal run | Notes |
|---|---|---|---|---|
| `quanfire-ai/multilingual-embedding` | e5-small | **published** | `prod-a70s30-fr` | Flagship general-multilingual. Predates this scheme; kept as-is (renaming a published repo breaks every inbound link). It **is** the `embed-general-multi` slot. **LEAK STATUS UNRESOLVED — the only published model with no verdict either way.** `prod-a70s30-fr` is a LoRA adapter, so it was produced by `qfme adapt`, whose split filter excluded a training pair only when its *positive* was a held-out positive. That is the same rule that invalidated all five numbers below. Its training data is aligned parallel text, which is the most bidirectional corpus shape there is, so the reverse-direction leak applies to it at least as strongly as to `embed-gov-indic` — and no document-level isolation was in force either. **No claim here is withdrawn, because this row has never carried a number; the exposure is on the HF card and in the fact that this is the model in production** (DocPro sidecar, playground). It has not been re-measured on a clean split. **PARTLY RESOLVED 2026-08-22 — off-box, by file comparison, no GPU needed.** The contamination question is a property of the split *files*, not the model, so it was answered by scanning the shipped training corpus `prod-a70s30-fr.jsonl.gz` (1,029,759 rows) against the eval sets, using the package's own `_membership_key`. Results: (1) **FLORES-200 global devtest — 0 of 15,180 sentences present in training**, exact or normalized, which clears the *Global all-pairs* and *French* instruments; (2) **`indic-aligned-nonpivot-eval` — 0.000% document overlap** against a document field populated on 100% of training rows, and 0.006% text overlap (60 rows) which inspection shows is **boilerplate recurrence across genuinely distinct documents** (42 of 60 are the single stock sentence `हे प्राचीन भारतातील सोळा महाजनपदांपैकी एक होते.`), i.e. the residual class the `without_held_out` comment already names — not the reverse-direction leak. Its linkage to the published number is **proven**, not assumed: `scratch_prod_flores.py:25` reads this exact file to produce `indomain_nonhi`. **The detector is not blind** — the same scan returned 60 hits on the Indic sets, so its zeros are measurements. (3) **`flores-indic-devtest.jsonl` — 0 of 10,120 sentences present in training**, which clears *FLORES non-Hindi*; (4) **`indic-aligned-eval` — 0.000% document overlap**, same boilerplate residual, and its linkage is now **proven too**: `scratch_prod_pivot.py:31` reads this exact file as `PIVOT_EVAL` to produce *hi-pivot r@10*. **All five published instruments are therefore accounted for, each traced to the code that produced it, and none shows the leak.** **No re-measure is currently indicated for this model** — which is the opposite of what this row assumed, and it releases the GPU window that was being reserved for it. **RESOLVED 2026-08-22 — this model does not share the defect.** The earlier instruction to treat its figures as unverified is lifted, with one honest limit: this is a *contamination* clearance, not a re-measure. It says the numbers are not leak-inflated; it does not re-derive them. The wider lesson stands — the five corrections below happened to be the models someone re-ran, not the models that were affected — and the way this one was settled is the cheap generalisation: **scan the split files before booking a GPU window.** |
| `quanfire-ai/embed-legal-en` | e5-small | **published** | `legal-indic-e1` | English Supreme Court **judgment** retriever. In-distribution Recall@1 0.309→0.545 (+76%, CIs disjoint). Trained only on statutory public-domain judgment text (Copyright Act §52(1)(q), headnotes excised, official court portals). Out-of-origin transfer to statutory text tested and **flat** — judgment-specific by design; scope is English judgments, not general legal. **CLAIM CORRECTED 2026-08-21 — the +76% was measured on a leaking split.** Re-run clean (`legal-indic-e1c`, same seed, corpus, hyperparameters): **0.3090 → 0.4935, +59.7%**, CIs [0.2891, 0.3296] vs [0.4716, 0.5154], still disjoint. This is the **best-surviving** of the three corrections: the 2,000 held-out pairs cover 1,379 documents and drop 48,304 pool pairs, halving training from 92,419 to **46,115**, and the model still reaches within 5 absolute points of the contaminated score on half the data. Not volume-matched — do not read the gap as the price of the leak alone. A second set of weights — the ones the clean figures above were actually measured on — is published as revision **`clean-2026-08-21`**; `main` is unchanged and still has no valid score of its own. See the *Which weights should I use?* section on the model card. |
| `quanfire-ai/embed-statute-en` | e5-small | **published** | `statute-en-e2` | English **central-statutory** (bare-Act) retriever — the register `embed-legal-en` measured itself *flat* on. In-distribution Recall@1 0.182→**0.269** (+48%, CIs disjoint [.165,.199]→[.250,.289]); on the un-gameable low-lexical-overlap slice (`<0.3`) 0.077→**0.177** (+131%). Trained on 858 Central Acts (Zenodo 5088102, CC-BY-4.0) mined into section-level pairs + adapter-mined hard negatives (positive-margin 0.05); §52(1)(q)(ii) makes bare-Act text train-safe for a **non-reconstructive** embedder → Apache-2.0 weights, corpus **not** redistributed. Statute-specific: judgments are `embed-legal-en`; state legislation, rules and notifications are unmeasured. **CLAIM CORRECTED 2026-08-21 — the +48% was measured on a leaking split.** Re-run clean (`statute-en-e2c`, same seed, corpus, hyperparameters, 2 epochs): **0.1815 → 0.2326, +28.1%**, CIs [0.1651, 0.1991] vs [0.2145, 0.2517], still disjoint. **Severely volume-confounded** — training falls from 32,432 pairs to **3,052** (90.6%; 33,252 was the pool left after the holdout, of which 32,432 actually trained). The 2,000 held-out pairs cover **591 documents** out of 858 Central Acts, so document-level holdout removes 97% of the pool (32,200 pairs). The lesson is a design one: on a document-poor corpus a *fixed number of held-out pairs* silently becomes a huge held-out document fraction. Hold out a fraction of documents instead. A second set of weights — the ones the clean figures above were actually measured on — is published as revision **`clean-2026-08-21`**; `main` is unchanged and still has no valid score of its own. See the *Which weights should I use?* section on the model card. |
| `quanfire-ai/embed-gov-indic` | e5-small | **published** | `gov-indic-e4-ep6` (v1.1) | **Cross-lingual** Indian **government press-release** retriever (16 Indian languages). In-distribution cross-lingual Recall@1 0.184→0.281 (**+53%**, CIs disjoint, also significant over v1.0's 0.235); right passage in the top-10 ~84% of the time. Trained on Press Information Bureau (PIB) releases, reused under PIB's reproduction policy (attribution, **no NC/SA**); non-reconstructive → Apache-2.0 weights. Specialist showcase of clean-provenance multilingual retrieval — other domains not validated; lowest-resource langs (Khasi/Nepali/Manipuri) thin. **v1.1** trains the same balanced corpus to 6 epochs (v1.0 was undertrained at 1 epoch); 13/16 langs improve but Gujarati & Odia regress vs v1.0 — both kept by revision tag. Runs: e1 (+8.7%, ns) → e2 (+16.2%, ns) → e3/v1.0 (+27.9%, significant) → e4/v1.1 (+53%, significant over base & v1.0). **CLAIM CORRECTED 2026-08-21 — the +53% was measured on a leaking split.** `pib.py` emits both directions of every language pair, and the old split filter excluded a training pair only when its *positive* was a held-out positive, so the reverse of a held-out pair trained freely. Re-run on a clean split (`gov-indic-e4c`, same seed, corpus, hyperparameters and 6 epochs): **0.1836 → 0.2518, +37.1%**, CIs [0.1651, 0.2038] vs [0.2307, 0.2741], still disjoint. The direction and the significance survive; the magnitude does not. **Not volume-matched** — the fix cuts training from 7,270 pairs to 2,115, because one press release yields many same-document pairs across 16 languages, so most of the pool shares a document with the held-out set. The shipped weights are unchanged; this corrects what we claim about them, and the honest reading is that this corpus is too document-poor to support a clean high-volume train. A second set of weights — the ones the clean figures above were actually measured on — is published as revision **`clean-2026-08-21`**; `main` is unchanged and still has no valid score of its own. See the *Which weights should I use?* section on the model card. |
| `quanfire-ai/rerank-statute-en` | e5-small (cross-encoder head) | **published** | `statute-reranker-v2-fulldata` (v2.0.0; v1.0.0 pins the earlier, unmeasured weights) | **First reranker.** Cross-encoder (full e5-small + scalar relevance head, not a LoRA adapter) that reorders a bi-encoder's top-k on English central-statutory text. Rides `embed-statute-en`'s exact corpus (858 Central Acts, Zenodo 5088102 CC-BY-4.0) + form-matched negatives → non-reconstructive, Apache-2.0 weights, corpus not redistributed. **Run 2 (box/CUDA, 1200 held-out queries): retrieve-then-rerank Recall@1 0.205→0.336 (+63.8%), paired bootstrap 95% CI [+0.106,+0.157], excludes 0.** Recall@100 ceiling 0.718 (headroom unspent → a v1.1 spends it). Run 1 had collapsed to random (−96.3%) on form-separable mined negatives; the fix draws negatives from other records' positives (form-matched) so train distribution == eval distribution. Published to HF v1.0.0, public, full-model repo (470 MB). Different objective from every published bi-encoder. **NUMBER WITHDRAWN 2026-08-21 — measured on a split with no holdout at all.** This model did *not* train through `finetune.py`; it trained through `internal/reranker/reranker_prototype.py`, which does `random.seed(0); shuffle(rows); eval=rows[:1200]; train=rows[1200:]` and excludes **nothing** — not by document, not by text, not even by positive. Replaying that exact split over `pairs-neg.jsonl` (35,252 rows): **0 of 1,200 held-out queries are unseen**, 100% share a document with training, 77.3% of held-out positives appear verbatim in training. The bi-encoder baseline in that harness is the *frozen* base, so the contamination lands entirely in the reranker arm — i.e. squarely on the +63.8% delta. Do not quote it. A clean re-measure is required to know what, if anything, is real. **RE-MEASURED 2026-08-22 — the model has a valid number for the first time.** Two clean runs on a document-isolated split (18 of 856 Central Acts held out; the log asserts 0 held-out rows whose positive, anchor, or document appears in training; 1,494 queries against a 1,205-passage pool, B=2,000 seeded). **Run v1 came back null** — Recall@1 0.0723 → 0.0823, delta +0.0100 (+13.9%), paired 95% CI **[-0.0027, +0.0228] spans zero**, failing the house gate — but that run carried two defects that both suppressed it: (a) a **vestigial filter** that cut training to 8,966 rows (26.3% of the published run's volume) by requiring mined negatives the trainer then never read, the filter having been made obsolete by the very form-matched-negatives fix that rescued run 1; (b) **no best-checkpoint tracking** — weights were saved from the final step, and its last 1,000 steps ran ~1.7× the epoch-mean loss. **Run v2 fixed both and clears the gate: Recall@1 0.0723 [0.0589, 0.0857] → 0.1064 [0.0910, 0.1218], delta +0.0341 (+47.2%), paired bootstrap 95% CI [+0.0207, +0.0482], excludes 0.** Ceiling 0.7557; of the queries whose gold is recoverable in the top-100, the reranker lifts **14.1%** to rank 1 (v1: 10.9%). Best checkpoint selected on a 600-row validation slice carved from *training* documents, never the eval set; the chosen checkpoint (val 0.2891) beat the final step (0.3052), so v1's save-the-last-step logic would again have shipped worse weights. **+47.2% is NOT a corrected version of the withdrawn +63.8% and must never be presented as one.** The +63.8% was measured on a split that excluded nothing (0 of 1,200 held-out queries unseen), so it measures nothing; there is no true value it was an inflation of. The two figures come from different evaluation sets entirely. This is reported from zero, not revised down. **Attribution caveat:** v1 → v2 changed *two* things at once (2.93× training volume and best-checkpoint selection), so the +13.9% → +47.2% movement cannot be attributed to either alone. v2 trained on 26,252 rows = 77.1% of the published run's volume (26,252 of 34,052) — closer to volume-matched than v1 but still not matched, so no delta here is 'the cost of the leak'. |
| `quanfire-ai/rerank-gov-indic` | e5-small (cross-encoder head) | **published** | `gov-reranker-v2-semihard` (v1.0.0) | **Cross-lingual government reranker** — reorders a retriever's top-k over Indian **government press-release** text in **16 Indian languages**. Full fine-tune of multilingual-e5-small with a single-logit ranking head (`max_length` 256, 2 epochs, lr 2e-5, seed 0, bf16/CUDA). Rides `embed-gov-indic`'s PIB corpus (reproduction policy, attribution, no NC/SA) → non-reconstructive, Apache-2.0 weights, corpus not redistributed. **Retrieve-then-rerank Recall@1 0.3083 → 0.4067 (+31.9%), paired bootstrap 95% CI [+0.0692, +0.1275], excludes 0** (1,200 cross-lingual queries, 1,096-passage pool, B=2,000 seeded). Lifts 42.9% of the recoverable queries (top-100 ceiling 0.9475) to rank 1. **The finding is the negative-hardness curve:** random negatives +10.5%, the retriever's *hardest* top candidates **−45.1% (a regression** — near-duplicate releases about the same event, so training on them teaches the model to demote correct answers), semi-hard negatives from ranks [20,100) **+31.9%** (shipped). The shipped CI's lower bound sits above the random-negative version's upper bound, so it strictly dominates. Aggregate across the mixed-language query set — **not** a per-language guarantee; other domains unvalidated. **NUMBER WITHDRAWN 2026-08-21 — measured on a split with no holdout at all.** Same defect as `rerank-statute-en`: it trained through `internal/reranker/gov_reranker_v2_prototype.py`, whose split is `random.seed(0); shuffle(rows); eval=rows[:1200]; train=rows[1200:]` with no exclusion of any kind. Replaying it over `pairs-e3.jsonl` (11,469 rows): **42 of 1,200 held-out queries unseen (3.5%)**, 96.4% share a document with training, 71.8% of held-out positives appear verbatim in training. For scale, the eulaw leak that started this audit left 7.4% unseen — the rerankers are worse. The negative-hardness curve (random +10.5% / top-hard −45.1% / semi-hard +31.9%) is a *relative* comparison across three runs on the identical split, so its shape may survive; its magnitudes do not. Do not quote them. **REPLACED 2026-08-21 — clean re-measure landed.** Retrained on a document-isolated split (264 of 1,505 documents held out; the log asserts 0 held-out rows whose positive, anchor, or document appears in training) and re-scored: retrieve-then-rerank **Recall@1 0.0626 [0.0528, 0.0728] → 0.1233 [0.1096, 0.1375], delta +0.0608 (+97.2%), paired bootstrap 95% CI [+0.0475, +0.0745], excludes 0** (2,254 cross-lingual queries, 1,077-passage pool, B=2,000 seeded). Weights published as revision **`clean-2026-08-21`**; `main` is unchanged and still has no valid score of its own. **The larger percentage is not an improvement and must not be quoted as one.** This is the only comparison in the whole audit that is close to volume-matched (9,200 clean rows vs 10,269 published = 89.6%), and what it shows is that the *first stage* collapses on an honest split — 0.3083 → 0.0626 — and a reranker's relative gain is largest exactly when its first stage is weakest. The number worth reading is the one that fell: of the queries whose gold is recoverable in the top-100, the reranker lifts **13.3%** to rank 1, down from the contaminated 42.9%. Ceiling 0.9475 → 0.9259. Only the shipped semi-hard arm was re-measured, so the negative-hardness curve still has no clean ordering — its three magnitudes remain withdrawn. **+97.2% WITHDRAWN 2026-08-23 — estimator bias, not a leak.** The split was clean (all three leak assertions zero), but `gov_reranker_v3_docsplit.py` has **no validation slice and no best-checkpoint tracking** — one unconditional `save_pretrained` at line 227, so the run shipped **last-step weights**. Best-checkpoint selection did not exist yet: it arrived with `statute_reranker_v2_fulldata.py` at 08-22 02:01, and this run finished 08-21 23:36. The same defect read *null* on statute v1 and *too high* here — opposite signs, which rules out a leak (leaks only flatter) and fingerprints estimator bias. **This row asserted a defensible clean number and was wrong to.** A re-run on v3's split with v2's validation harness is required before any gov reranker number is quotable. Caught by mas-ai's eval-attest card; the registry row, not the card, was stale. **RE-MEASURED 2026-08-23 — the gov reranker has a valid number for the first time: Recall@1 0.0626 [0.0528, 0.0728] → 0.1291 [0.1154, 0.1433], delta +0.0665 (+106.4%), paired bootstrap 95% CI [+0.0532, +0.0807], excludes 0** (2,254 cross-lingual queries, 1,077-passage pool, B=2,000 seeded). Produced by `gov_reranker_v4_bestckpt.py`, which adds to v3's clean split the validation harness v3 lacked: a 600-row slice carved from *training* documents, with its own three assertions that it never touches the eval set. **The withdrawal was correct and the run proves it:** the selected checkpoint (e1s3784, val loss 0.0560) beat the final step (0.1856) by **3.3×**, so v3's single unconditional save shipped weights from the worst point of the run. Of the queries whose gold is recoverable in the top-100, the reranker lifts **13.9%** to rank 1 (ceiling 0.9259). **+106.4% is NOT an improvement over the withdrawn +97.2% and must never be read as one.** v4 trades ~10% of documents to the validation slice, so it trains on 8,216 rows against v3's 9,200 — not volume-matched — and the two numbers come from different estimators, which is the entire point. The withdrawn figure measures nothing; this one replaces it. **Leak re-verified independently off-box 2026-08-23:** the split was replicated from the corpus (2,254 eval / 8,216 train, both matching the run exactly) and re-tested under NFKC-casefold-punctuation normalization, which the run's exact-string exclusion cannot see. Exact leaks 0, confirming the run's assertion; **normalized near-duplicates 5 rows (0.22% of eval)**. Bounded worst case — if all five were pure memorization — Recall@1 falls to 0.1269 and the delta to +102.9%, still far clear of the CI floor. The reverse-direction leak that voided the bi-encoders is structurally closed here: the exclusion set is the **union of both sides** of every training row, so an eval anchor appearing as a training positive is caught. |
| `quanfire-ai/embed-legal-indic` | e5-small | planned | — | **Cross-lingual** Indian legal (en↔hi/ta). Separate from `embed-legal-en`: needs Indic legal parallel data before language becomes an adapted axis. Not the English judgment model. |
| `quanfire-ai/embed-finance-indic` | e5-small | planned | — | CA firms: accounting / tax / audit / filings. |

## Two growth axes

- **Language breadth** deepens the *one* flagship (`multilingual-embedding`) — a
  single strong multilingual model beats a scatter of per-language ones. Breadth =
  more parallel data into the flagship, not more repos.
- **Domain depth** ships *separate* specialists, one per `domain`×`scope`. This is
  the defensible work; generic models are free.
